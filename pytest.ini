[pytest]
# pythonpath sorgt dafür, dass 'from src import agent' beim Aufruf von pytest
# aus dem Projekt-Root zuverlässig funktioniert, ohne dass wir das Projekt
# als Package installieren müssen.
pythonpath = .
testpaths = tests
