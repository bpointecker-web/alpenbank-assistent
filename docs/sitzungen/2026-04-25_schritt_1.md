# Sitzungsprotokoll – 2026-04-25 – Schritt 1

## Was wurde aufgebaut

- Projekt-Setup: Ordnerstruktur (`data/`, `src/`, `scripts/`, `tests/`,
  `docs/{reviews,retros,sitzungen}/`), `requirements.txt`, `.env.example`,
  `.gitignore`, `README.md`.
- Virtuelle Umgebung `.venv/` mit Python 3.13 und allen Abhängigkeiten.
- `src/chat.py`: Logikmodul mit drei Funktionen
  (`add_message`, `extract_response_text`, `send_to_claude`),
  Konstanten `MODEL`, `SYSTEM_PROMPT`, `MAX_TOKENS`, `ALLOWED_ROLES`.
- `tests/test_chat.py`: 13 Unit-Tests + 1 übersprungener Integrations-Test.
- `pytest.ini`: Konfiguration, damit `from src import chat` funktioniert.
- `src/app.py`: Streamlit-Chat-Oberfläche.
- Code-Review (`docs/reviews/schritt_1_review.md`) und Retrospektive
  (`docs/retros/schritt_1_retro.md`).

## Welche Dateien wurden erstellt oder geändert

**Erstellt:**
- `requirements.txt`
- `.env.example`
- `.env` (lokal, nicht versioniert – enthält den API-Key des Nutzers)
- `.gitignore`
- `README.md`
- `pytest.ini`
- `src/__init__.py`
- `src/chat.py`
- `src/app.py`
- `tests/__init__.py`
- `tests/test_chat.py`
- `docs/reviews/schritt_1_review.md`
- `docs/retros/schritt_1_retro.md`
- `docs/sitzungen/2026-04-25_schritt_1.md` (dieses Protokoll)

**Geändert:** keine bestehenden Dateien.

## Was funktioniert

- `pytest`: 13 grün, 1 erwartungsgemäß übersprungen, Laufzeit < 0,1 s.
- Streamlit-App lässt sich starten mit
  `.venv/Scripts/python.exe -m streamlit run src/app.py`.
- Manueller Browser-Test bestanden: Eingabe → Claude antwortet auf Deutsch.

## Stand für die nächste Sitzung

**Fertig:** Schritt 1 aus KONZEPT.md vollständig.

**Als Nächstes (Vorschlag):**

1. **Datenerzeugungs-Skript** `scripts/daten_erzeugen.py` – erzeugt die
   SQLite-Datenbank `data/controlling.db` (Kostenstellen, Konten,
   Buchungen 2024+2025) und die fünf Textdokumente unter
   `data/dokumente/`. Voraussetzung für Schritt 2 und 3.
2. Optional vor Schritt 2: `pip freeze > requirements.lock`, damit die
   bisher installierten Versionen reproduzierbar sind.
3. **Schritt 2 aus KONZEPT.md**: RAG-Teil – Dokumente einlesen, in ChromaDB
   ablegen, semantische Suche, Quellenangaben in Antworten.

**Empfehlung:** mit dem Datenerzeugungs-Skript starten, weil es kurz und
in sich abgeschlossen ist und die Grundlage für die nächsten zwei Schritte
liefert.
